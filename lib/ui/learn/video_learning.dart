import 'package:flutter/material.dart';

class VideoLearingUI extends StatelessWidget {
  const VideoLearingUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

